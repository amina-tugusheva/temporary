#include "iostream"
#include "string"
using namespace std;
int main()
{
	string str;
	int k = 0, pos = 0;
	int s = 0;
	getline(cin, str);
	while (k < str.length())
	{
		if (str[k] >= '0' && str[k] <= '9')
		{
			s += 1;
		}
		
		k++;
	}
	cout << s;
	return 0;
}
